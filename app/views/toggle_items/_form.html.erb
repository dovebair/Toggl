<%= form_for([@toggle_list, @toggle_list.toggle_items.build]) do |f| %>
 <%= f.text_field :content, placeholder: "New Toggle" %> 
  <%= f.text_field :style, placeholder: "border: solid 1px red" %>
<%= f.submit %>
<% end %>